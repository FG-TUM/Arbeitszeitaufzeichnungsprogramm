# bin

This folder provides executable scripts, intended to be added to $PATH, that allow to run the program directly from the command line on different operating systems.

They work by activating the appropriate environment and then executing the main program script.